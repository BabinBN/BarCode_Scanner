_schema-version: "3.2"
ID: barcode
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: barcode-destination-content
  type: com.sap.application.content
  requires:
  - name: barcode-destination-service
    parameters:
      content-target: true
  - name: barcode-repo-host
    parameters:
      service-key:
        name: barcode-repo-host-key
  - name: barcode-uaa
    parameters:
      service-key:
        name: barcode-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: barcode_html_repo_host
          ServiceInstanceName: barcode-html5-service
          ServiceKeyName: barcode-repo-host-key
          sap.cloud.service: barcode
        - Authentication: OAuth2UserTokenExchange
          Name: barcode_uaa
          ServiceInstanceName: barcode-xsuaa-service
          ServiceKeyName: barcode-uaa-key
          sap.cloud.service: barcode
        existing_destinations_policy: update
  build-parameters:
    no-source: true
- name: barcode-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: barcode-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - barcode.zip
      name: barcode
      target-path: resources/
- name: barcode
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: barcode-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: barcode-destination-service
    service-plan: lite
- name: barcode-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: barcode-xsuaa-service
    service-plan: application
- name: barcode-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: barcode-html5-service
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
